# Compiled binaries
bin/

# macOS metadata
.DS_Store

# IDE
.vscode/

# Claude Code local state
.claude/

# Captured images
images/
calibration/calibration_*/
calibration/calib_result/

# Python virtualenvs
calibration/.venv/
backends/.venv/
site/

# Vendor SDKs and documentation — excluded via .git/info/exclude
# so they stay visible to coding agents' @ file picker.
# ArenaSDK_*/
# PDH016S_*_Documentation*/

# Scratch images
*.png

# Capture and preview artefacts. `stream`, `depth-preview` and `capture` write these
# into the working directory by default, so they accumulate during any bench session
# and are easy to commit by accident. Ignored, not deleted.
*.mp4
*.pgm
depth_preview_*
stream_*
capture_*
inspection_demo/
tending_captures/
